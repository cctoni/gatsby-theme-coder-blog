diff --git a/node_modules/@mdx-js/mdx/index.js b/node_modules/@mdx-js/mdx/index.js
index 5e2b435..88bdc90 100644
--- a/node_modules/@mdx-js/mdx/index.js
+++ b/node_modules/@mdx-js/mdx/index.js
@@ -54,7 +54,8 @@ function esSyntax() {
   methods.splice(methods.indexOf('paragraph'), 0, 'esSyntax')
 }
 
-function createMdxAstCompiler(options) {
+function createMdxAstCompiler(_options = {}) {
+  const options = Object.assign({}, DEFAULT_OPTIONS, _options)
   const mdPlugins = options.mdPlugins
 
   const fn = unified()
@@ -110,7 +111,8 @@ function applyHastPluginsAndCompilers(compiler, options) {
   return compiler
 }
 
-function createCompiler(options) {
+function createCompiler(_options = {}) {
+  const options = Object.assign({}, DEFAULT_OPTIONS, _options)
   const compiler = createMdxAstCompiler(options)
   const compilerWithPlugins = applyHastPluginsAndCompilers(compiler, options)
 
@@ -150,5 +152,6 @@ compile.sync = sync
 module.exports = compile
 exports = compile
 exports.sync = sync
+exports.createCompiler = createCompiler
 exports.createMdxAstCompiler = createMdxAstCompiler
 exports.default = compile
